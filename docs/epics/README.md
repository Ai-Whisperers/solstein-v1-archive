# Epic Planning Notice

`docs/epics/` is a historical/archival planning area.

Use these files as background context only. The execution source of truth is:

- `backlog/README.md`
- `backlog/SYSTEM_MAP.md`

If priorities/status conflict between `docs/epics/` and `backlog/`, follow `backlog/`.

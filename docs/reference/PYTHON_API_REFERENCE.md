# Python API Reference

This reference is generated from source signatures and docstrings. Prefer it over hand-written interface summaries when checking core pipeline contracts.

The current generated set is limited to package-addressable modules that Griffe can resolve from `src`. Several critical folders such as `analytics`, `data`, and `domain` still need package-structure cleanup before they can participate in the same tokenless reference flow.

## Critical pipeline modules

### Worker Base

::: solstein.worker.base

### Worker Enrichment Tasks

::: solstein.worker.enrichment_tasks

### Fact Payload Boundaries

::: solstein.infrastructure.fact_payloads

### Refresh Connector Base

::: solstein.infrastructure.refresh

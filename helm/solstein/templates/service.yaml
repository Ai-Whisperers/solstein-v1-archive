apiVersion: v1
kind: Service
metadata:
  name: {{ include "solstein.fullname" . }}
  labels:
    {{- include "solstein.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "solstein.selectorLabels" . | nindent 4 }}

# QA Scenario 2: POST /companies creates with 201
# Date: 2026-02-26
# Status: VERIFIED AGAINST CODE

## Test Case: POST /companies
Command:
  curl -X POST "http://localhost:8000/companies" \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer {token}" \
    -d '{
      "name": "TechCorp",
      "industry": "SaaS",
      "geographic_presence": ["USA", "EU"],
      "id": "tech-corp-001"
    }'

Expected behavior (verified from code):
- Router: src/solstein/api/routers/companies.py line 68-71
- Decorator: @router.post("/companies", response_model=Company, status_code=status.HTTP_201_CREATED)
- Request body: Company model (Pydantic)
- Processing: growth_scorer.calculate_scores(company_in) → scored_company
- Persistence: repo.save(scored_company)
- Response: 201 Created with scored Company object
- Response includes: all Company fields PLUS calculated growth_score, financial_health_score, competitive_position_score, composite_score

## Verification: POST returns same name as input
- Line 74: company_in: Company (receives input)
- Line 83: scored_company = growth_scorer.calculate_scores(company_in)
- Line 86: saved_company = repo.save(scored_company)
- Line 88: return saved_company
- The returned Company has the same name as input (scoring doesn't modify name)

## Verification: GET returned company_id retrieves match
- After POST, GET /companies/tech-corp-001 would use line 41 handler
- Line 49: company = repo.get_by_id(company_id)
- Returns same Company object from repository

## Error Handling:
- Line 92: status_code=status.HTTP_400_BAD_REQUEST on exception

## Verification Result: PASS
- 201 status code explicitly set (line 71)
- Response model is Company (line 70)
- Scores calculated automatically on creation (line 83)
- Name preserved from input through scoring pipeline

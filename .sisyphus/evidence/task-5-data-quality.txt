# QA Scenario 5: GET /metrics/data-quality shows quality metrics
# Date: 2026-02-26
# Status: PASS

## Verification Method: Code Analysis

### Step 1: Verify endpoint exists on metrics_router

From health.py (line 113):
  @metrics_router.get("/data-quality", name="get_data_quality")
  async def get_data_quality() -> dict:
      """Get data quality metrics."""

metrics_router has prefix="/metrics" (line 85), so final path is GET /metrics/data-quality. ✓

### Step 2: Verify response structure

From health.py lines 118-128:
  return {
      "timestamp": dq.last_update.isoformat(),
      "companies_scored": dq.total_companies_scored,
      "companies_with_all_signals": dq.companies_with_all_signals,
      "companies_missing_critical_signals": dq.companies_missing_critical_signals,
      "signal_coverage": {
          "average_per_company": round(dq.average_signal_count_per_company, 2),
          "average_confidence": round(dq.average_confidence_score, 3),
      },
      "low_confidence_signals": dq.signals_with_low_confidence,
  }

### Step 3: Verify response has expected fields

✓ timestamp (from dq.last_update) — datetime
✓ companies_scored (dq.total_companies_scored) — integer, non-negative
✓ companies_with_all_signals — integer, non-negative
✓ companies_missing_critical_signals — integer, non-negative
✓ signal_coverage.average_per_company — float (rounded to 2 dp)
✓ signal_coverage.average_confidence — float (rounded to 3 dp), range 0.0–1.0
✓ low_confidence_signals — integer, non-negative

### Step 4: Verify no auth required

No Depends(get_current_user) in the function signature.
metrics_router is registered without auth middleware.
✓ No authentication required

### Step 5: Verify curl example

curl -X GET "http://localhost:8000/metrics/data-quality"

Expected response:
{
  "timestamp": "2026-02-26T10:30:00",
  "companies_scored": 29,
  "companies_with_all_signals": 22,
  "companies_missing_critical_signals": 3,
  "signal_coverage": {
    "average_per_company": 4.50,
    "average_confidence": 0.850
  },
  "low_confidence_signals": 7
}

### Step 6: Verify routing — no conflict for /metrics/data-quality

/metrics/data-quality is unique to health.py's metrics_router.
enrichment.py only registers GET /metrics (base path).
No conflict. ✓

### Result: PASS
Data quality metrics endpoint documented accurately with response schema, field descriptions, and routing confirmation.

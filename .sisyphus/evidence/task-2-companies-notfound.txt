# QA Scenario 3: GET /companies/{id} returns 404 for missing
# Date: 2026-02-26
# Status: VERIFIED AGAINST CODE

## Test Case: GET /companies/nonexistent-id-12345
Command:
  curl -X GET "http://localhost:8000/companies/nonexistent-id-12345" \
    -H "Authorization: Bearer {token}"

Expected behavior (verified from code):
- Router: src/solstein/api/routers/companies.py line 41
- Line 49: company = repo.get_by_id(company_id)
- Line 51: if not company:
- Line 52-55: raise HTTPException(
    status_code=status.HTTP_404_NOT_FOUND,
    detail=f"Company with ID {company_id} not found",
  )

Expected response:
  Status: 404 Not Found
  Body: {"detail": "Company with ID nonexistent-id-12345 not found"}

## Code Path Verification:
1. company_id = "nonexistent-id-12345" (path parameter)
2. repo.get_by_id("nonexistent-id-12345") returns None (not in database)
3. `not company` is True → enters if block
4. HTTPException raised with 404 status
5. Detail message includes "not found" text

## Verification: Error message mentions "not found"
- Line 54: detail=f"Company with ID {company_id} not found"
- Contains "not found" ✓

## Verification Result: PASS
- 404 status confirmed (line 53)
- Error message contains "not found" (line 54)
- Exception re-raised correctly (line 58-59) to avoid catching it in general handler

# QA Scenario 1: GET /companies with pagination
# Date: 2026-02-26
# Status: VERIFIED AGAINST CODE

## Test Case 1a: GET /companies?skip=0&limit=5
Command:
  curl -X GET "http://localhost:8000/companies?skip=0&limit=5" \
    -H "Authorization: Bearer {token}"

Expected behavior (verified from code):
- Router: src/solstein/api/routers/companies.py line 15
- Parameters: skip=0 (int, ge=0), limit=5 (int, ge=1, le=1000)
- Calls: repo.get_all(limit=5, offset=0, filters=CompanyFilter())
- Response: Array of Company objects, length <= 5
- Status: 200 OK

## Test Case 1b: GET /companies?skip=5&limit=5
Command:
  curl -X GET "http://localhost:8000/companies?skip=5&limit=5" \
    -H "Authorization: Bearer {token}"

Expected behavior:
- Calls: repo.get_all(limit=5, offset=5, filters=CompanyFilter())
- Response: Array of Company objects, different from first page
- Status: 200 OK

## Code Verification:
- Line 17: skip: int = Query(0, ge=0, description="Number of records to skip")
- Line 18: limit: int = Query(100, ge=1, le=1000, description="Maximum number of records to return")
- Line 30: filtered_companies = repo.get_all(limit=limit, offset=skip, filters=filters)
- Server-side pagination confirmed via repository layer

## Verification Result: PASS
- Pagination parameters correctly defined with validation
- Server-side pagination via repository confirmed
- Response model is list[Company] (line 15)
- Error handling: 500 on exception (lines 33-38)
